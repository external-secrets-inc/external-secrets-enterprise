apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  labels:
    external-secrets.io/component: controller
  name: pingidentityfederations.identity.federation.external-secrets.io
spec:
  group: identity.federation.external-secrets.io
  names:
    categories:
    - external-secrets
    - external-secrets-federation
    kind: PingIdentityFederation
    listKind: PingIdentityFederationList
    plural: pingidentityfederations
    singular: pingidentityfederation
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              environmentId:
                description: EnvironmentID is the PingOne environment ID (UUID)
                type: string
              region:
                description: Region is the PingOne region (e.g., "com", "eu", "asia",
                  "ca")
                enum:
                - com
                - eu
                - asia
                - ca
                type: string
            required:
            - environmentId
            - region
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
