apiVersion: generators.external-secrets.io/v1alpha1
kind: Fake
metadata:
  name: test-fake-generator
  namespace: default
spec:
  # Optional: specify a controller if you have multiple ESO controllers
  # controller: my-controller

  # Define the static data that will be returned by the generator
  data:
    username: "test-user"
    password: "test-password"
    api-key: "1234567890abcdef"
